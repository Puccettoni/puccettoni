# Internal tool folder (empty)
.sixth/

# OS metadata
Thumbs.db
.DS_Store

# Editor backups / temp
*.swp
*.bak
*~

# backups locais e arquivos temporarios
*.backup-*
test_write.txt

# dependencias node + tooling de dev (site e estatico; motion carrega via CDN)
# mantidos so local para nao disparar build no Vercel nem poluir o repo
node_modules/
package.json
package-lock.json
.claude/
